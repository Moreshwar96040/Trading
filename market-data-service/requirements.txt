fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
sqlalchemy>=2.0,<3
psycopg2-binary>=2.9,<3
pandas>=2.0,<3
yfinance>=0.2.40
apscheduler>=3.10,<4
pydantic-settings>=2.2,<3
scikit-learn>=1.3,<2
httpx>=0.27,<1
# Exness/MT5 bridge: talks to a locally running MetaTrader 5 terminal over IPC.
# Windows-only — the marker keeps Linux/Docker installs of this service working
# (app/providers/mt5.py imports it lazily and degrades to a status when absent).
MetaTrader5>=5.0.45; sys_platform == "win32"

